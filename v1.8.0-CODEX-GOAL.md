# Codex 完整目标：直接完成 OpenJatoBID v1.8.0 生图模式

**R3｜2026-09-26｜整份作为一次任务提交，不需要逐项发送实施编号。**

---

你现在接手 OpenJatoBID 的 v1.8.0 生图模式。我的业务范围和设计方向已确认，请直接完成既有代码基础上的实现、测试、修复、只读审查与交付，不再只交规划，也不要求我逐项说“继续”。

## 1. 目标与输入

本地项目：`D:\Documents\OpenJatobid`。开发分支核对基准：`v1.8.0-Image-generation`，远程已核查提交为 `ab6755831ceb52a0863af43d9656cea671b7d2de`。这不是回退目标；保留本地更近成果和未提交修改。

先读取适用的 AGENTS、开发说明、项目二次开发 Skill，以及已有 `tasks/plan.md`、`tasks/todo.md`。本次使用该工作流的完整执行方式，v1.8.0 的常规 plan → build → verify → review 已由本提示词授权连续推进。

随后按顺序读取：

1. `docs/v1.8.0-spec-final.md`：R3 完整业务权威，不需要旧 Spec 补齐。
2. `docs/v1.8.0-ui-handoff-final.md`：R3 页面、组件、交互、勘误和响应式要求。
3. `docs/assets/v1.8.0-r3/design-assets.json` 及其列出的 11 张独立设计图；看图，不只读文件名。四个 `.preview.html` 只作排版参考，不是成品应用。
4. `docs/v1.8.0-source-baseline.json`：七个精确来源 ID、出处和 URL，仅配置，不是初始内容。
5. Spec 第 16 节的 AC01–AC60 验收合同。

旧 `v1.8.0-spec.md`、补充 Spec、旧任务书及旧 Handoff 的 v1.8.0 内容已由上述 R3 替代。只对本版过期说明加替代标记，不删除其他文档，不改写全局 Skill 的权限规则。

## 2. 最终必须出现的产品效果

在原客户端内完成一级“生图模式”，只简单模式，三个二级入口为“AI 生图／提示词中心／我的作品”。保持原应用壳与其他模块，不重新做一个网站。

完成以下闭环，而不是只做按钮或模拟页面：

- 中文描述、八类任务预设、文生图；1–4 张参考图并区分主体／风格／构图／色彩；实际比例、数量和画质。
- 当前图片局部选区编辑、新版本、允许区域外像素保护。
- 图片反推中文提示词，提示词优化、前后对比、主动应用和撤销。
- 内置七个来源的真实中文参考库；来源级新增、编辑、启停、删除、地址检测、刷新和升级保护。
- 我的提示词完整管理、常用风格、作品收藏／搜索／详情、父子版本、草稿和任务恢复。
- PNG／JPG／WEBP 真编码导出；有语义意义的真实像素层、图层检查和 PSD 文件交付。

模型与我项目完全匹配：**文本、视觉反推、优化和翻译均复用原“文本模型” `gpt-6-sol`；生图与编辑复用原“生图模型”当前选择的 `gpt-image-2` 或 `gpt-image-2-1k`。** 两类模型均走已有国内中转。不让我再次填写已有 Key／Base URL，不新增必填视觉模型，不擅自切换渠道或升档。文本视觉能力须真实传图片，不能只传文件名。

不做专业模式、无限画布、视频、音频、多 Agent、插件市场、AI／AIT、自动投标配图联动、独立镜像服务器或新模型商城。

## 3. 一次执行的内部步骤

以下步骤是你内部的实施顺序，不是给用户的新审批清单。每个步骤完成就继续，范围内问题自行修复；每项输入、输出和关键约束如下。

### 步骤 A：接续代码、建立可比较基线

**输入：**当前仓库、R3 资料、真实 Git 状态、已有测试与运行配置结构。  
**输出：**已有 plan 的差距摘要、现有 todo 的真实状态、需要保留与补齐的入口、改前测试基线。  
**约束：**只读核对分支／HEAD／未提交修改，不自动 fetch、pull、checkout、reset、创建分支或删除修改；配置秘密只在 Main 受控读取，不输出其值。

确认已有 imageStudio 服务、表、路由、原提示词保留、格式编码和 PSD 工具可复用。按职责拆页面，不因旧 UI 不好看就重建所有后端。在当前客户端已有模式下扩展，不引入飞书或其他外部低代码流程。

只在用户工作区的目标分支一致时继续源代码实现；若分支不一致，先完成安全只读核对，不擅自切换或把整个改动写到 main。不要让简单的文件命名差异成为全局停工理由。

### 步骤 B：扩展既有模型、任务与数据合同

**输入：**configStore、aiService、现有队列、IPC/preload/types、SQLite 和 R3 模型规则。  
**输出：**可复用两类配置的文字／视觉／生成／编辑通路，明确任务状态与资产合同，安全迁移及测试。  
**约束：**所有模型请求仍走现有 aiService；不得 Renderer 直连；提交配置冻结且与实际使用一致；不把 Key 持久化进任务。

正常尝试读取 `https://www.yuque.com/heligong/rp6olb`，并追踪当前有效请求。读不到时使用现有代码与可信脱敏样本，缺少合同的能力单独标待验证，不猜多个端点反复试费。

明确两种图片 ID 的真实支持，不按 `1k` 推断所有尺寸。只发送有依据的参数；缺少 mask、多图或特定输出能力时如实说明，不造 fake 实现。最早核对多图、局部编辑和分层候选相关风险，但不能让一项风险挡住全部页面和数据工作。

备份一致的数据库和必要文件引用后做向前迁移；先在隔离副本验证。不要重建用户数据库，不把测试作品写进真实投标项目。

### 步骤 C：实现可用参考库、来源管理与升级保护

**输入：**七源配置、真实上游内容和版本、使用条件、既有 Store。  
**输出：**随包初始中文内容、真实来源管理服务／UI、内容同步与升级合并、AC25–AC42 相关测试。  
**约束：**不得把七个 URL 当“开箱可用”；不得植入虚假示例充库；用户修改、停用、删除、个人副本必须保留。

固定上游版本并核对 manifest 与数据哈希；准备七源真实中文标题、标签、正文和原文／出处，记录数量、翻译版本及未纳入原因。优先复用上游中文字段和当前可用文本处理能力。不要在每台员工电脑首启时秘密做全量付费翻译，也不要以两三条自编例子掩盖资源未准备。

只有允许分发的示例图才能随包。文本可用与图片预览加载分开，图片失败有占位。GitHub Raw 是沿用默认线路，不标成“大陆稳定镜像”；首版无需另部署服务器，备用地址默认为空，用户可以填写真实 JSON 镜像。

区分默认配置、用户覆盖／删除标记、内容快照、个人副本。支持内置与自定义源增删改启停、单源刷新、检测、主动恢复。改址／删除后的晚响应要丢弃；异常骤减、空 JSON、失效地址不得清空旧快照。升级只更新未被用户覆盖的默认字段。

### 步骤 D：按已认可图稿实现全部页面与提示词工作流

**输入：**11 张图、R3 Handoff、业务合同、现有组件。  
**输出：**三个页面、局部／反推／优化／个人保存／常用风格／来源管理等弹窗及状态，实际可操作 UI。  
**约束：**不再做“一个文本框＋生成按钮”的原型；不引入 Tailwind 或整套新 UI 框架；不把图稿省略菜单或误写文案照搬。

沿用灰蓝视觉、结果主区域和宽屏三栏；窄窗收起右栏，输入和结果可用。按 Handoff 修正“官方”、虚构模型、种子、投标封面、公司自动水印、全局搜索／教程／分享等非需求内容。

优化先对比再应用；反推和保存不自动生图。草稿修订、输入图片变化与异步结果绑定，避免过期结果覆盖。个人库通过用途元数据兼容扩展，不只按“生图提示词”组名筛选；重命名组仍能找到。常用风格不清空主体和参考图，不切模型。

没有独立 PNG 的异常／空状态按既有组件和 Handoff 实现，不另外把这些状态变成新的确认门。

### 步骤 E：补齐生图、修改、版本、恢复和下载

**输入：**实际渠道适配、受管资产、Main 任务、页面事件。  
**输出：**真实生成、参考角色进入请求、局部修改、新版本、作品管理及三格式下载闭环。  
**约束：**原图不覆盖、参考图不静默丢弃、计费提交不盲重试、取消不影响其他业务。

选择真实结果 ID；新图默认显示，用户主动浏览旧图时提示新结果而不抢焦点。普通多结果是兄弟作品，不误建父子关系。“继续创作”带当前图，而不是仅拷贝 prompt。

区分已发出、下载中、部分成功、明确失败、待确认和取消；保存成功子图。只下载失败就重试下载，不重生。恢复未完成任务时根据可查询 ID 和安全凭据引用处理，不偷偷重新计费。

遮罩用原图坐标；原生 mask 或标注参考方案必须对齐结果，再由本地合成保护允许区之外像素。错误保留选区和描述。删除作品不破坏子版本引用；软删除不宣称释放磁盘。

### 步骤 F：交付真正像素分层 PSD

**输入：**原图、当前视觉模型语义候选、本地图像处理、已有 sharp／ag-psd。  
**输出：**可用语义候选层、边缘修正、真实 RGBA、显隐／顺序／合成预览、PSD 文件及证据。  
**约束：**不把视觉理解当精确分割；不新增必填云模型；不以纯手工完整涂图或复制整图称自动拆层。

尽早实现并验证当前能力内的语义候选和本地细化。产品承诺是有意义的像素分层，不是原始设计工程。候选边缘允许用户修正；未补全被遮挡背景时明确提示透明孔洞。无补全的完整层集重组必须在标准化像素空间复现原图。

拒绝空层、重复整图、任意网格切片、仅改少量像素或隐藏整图掩盖错误。图层预览和 PSD 使用同一组像素数据；中文名称、透明、尺寸、顺序正确。文字像素层不能标成可编辑文字。

真实照片、产品图和含文字样本分别检查。PSD 写入／回读只是本地测试，还要有 Photoshop 打开和独立图层操作的实机证据。当前环境没有软件时保留待实机验证，其他功能继续；不能以 8×6 色块测试替代本功能。

### 步骤 G：验证、修复与回归

**输入：**AC01–AC60、实际 package scripts、现有测试和目标运行环境。  
**输出：**分层测试报告、真实业务截图、实际接口证据、迁移和 PSD 检查结果，以及修复后的复测记录。  
**约束：**模拟、设计稿、登录页、库回读与真实验收分开；不为了变绿删除逻辑、跳过失败或绕过授权。

先聚焦单测／请求构造／像素，再语法、构建、Electron 业务和必要相关回归。按 Node／Electron 实际 ABI 执行，不臆造 npm test 脚本；客户端命令在 client 下。大像素处理不得堵主进程，列表不能加载全部高清图。

新装无公网条件下验证七源中文可用，升级保留用户覆盖与删除，来源竞态测试必须覆盖。两个图片 ID、视觉输入、多图及编辑做限额内真实小样本，异常分支主要靠模拟；不故意付费制造超时。

实际检查 1440×900、1280×800、1024×768（受既有最小窗约束）及 125% 缩放；不要全屏缩小截图冒充响应式。原 v1.7.5、对话、个人提示词、授权、埋点和更新行为必须回归。报告依赖风险时与基线区分，不执行 npm audit fix --force。

### 步骤 H：只读审查和一次性交付

**输入：**目标范围、最终 diff、测试证据与未完成项。  
**输出：**实际改动、配置复用、源资源与权利、迁移恢复、截图、分层报告、审查结论和剩余限制。  
**约束：**有 reviewer 时独立只读审查，没有则说明不是独立审查。范围内发现的问题自行修复、重验，不逐项等待用户批准。

真实完成的内容与待外部验证项分开报告；对本轮阻断项继续修复，不用“已写代码”或“构建通过”结束整个目标。更新 client/package.json 及对应 lock 到 1.8.0，版本一致但不替代验收；不改管理端版本。

所有运行证据放入一个本轮 evidence 目录，关联 AC 用例。任务执行状态仅在原 todo，测试报告不是另建开发状态总账。更新旧 v1.8.0 文档的替代提示，减少后续误用。

## 4. 操作和费用边界

本提示词被我提交执行后，允许修改本版所需业务代码、必要最小依赖、数据迁移、测试和文档；允许正常构建、非发布的本地验证。没有创建／切换分支、提交、推送、合并、rebase、reset、tag、发布或更改 Release／Worker／R2／签名体系的授权。验证打包资源可使用明确 `publish never` 的本地方式，不触发线上 workflow。

真实中转验证仅使用已有配置与非敏感测试素材，默认沿用前版上限：**本次新增文本请求最多 6 次、生图／编辑请求最多 6 次，每次生图最多 1 张，重试计入次数**。已有更低额度优先。请求前记录内部测试清单并累计次数，不必为每个限额内请求重新问我；不能因为 API Key 存在就无限测试。

次数不是价格承诺。无法控制输出、服务存在额外收费、需要新账户／服务或超限时，停该外部操作，继续本地实现并集中说明缺口。初始资源海量翻译不自动使用该中转限额；优先已有中文内容／本轮授权文本能力，超出范围如实记录，不假装翻译已完成。

密钥不得打印、写入仓库、日志、设计稿、测试报告或来源下载请求；业务模型保持原端点，不把语雀文档地址当 API 地址。

## 5. 完成判定与最后回复

不能把以下内容当作完成：只写计划、只做骨架、只有七源 URL、只有 mock 出图、只有 PSD 写入库、只有登录页截图、只有 build 通过、版本号已改。

请在已授权范围内连续工作到目标实现与可执行验证、范围内修复结束。真正外部阻塞只阻塞相应能力，不重开整套 PRD／UI 确认，不把逐任务执行责任交回我。

最后中文集中交付：
1. 已实现功能及用户可见效果；
2. 实际修改文件与现有能力复用；
3. 运行入口、实际截图和证据目录；
4. U／I／E／R／F 分层通过、失败、阻塞项；
5. 必须由我处理的真实外部条件（如确有）；
6. 未做发布、推送或其他未授权操作的事实说明。

**现在执行整个 Goal，不停在规划阶段。**
