# v1.7.4 目录自然拆分与混合层级实施记录

规格依据：[v1.7.4-spec.md](../../v1.7.4-spec.md)。软件版本仍为 1.7.3；未提交、推送、合并或发布。实施基线为本地分支 `0920v1.7.3-textedit`、提交 `2c3d7b9dc6672b706c956500afbac177857e8283`，未回退到规格书引用的远端审计提交。规格书作为用户未跟踪文件保持原样。

| 步骤 | 输入 | 输出与关键约束 | 实际验证 |
| --- | --- | --- | --- |
| 1. 基线与计划 | 根目录 AGENTS、开发说明、规格及 Git 状态 | 在 `tasks/todo.md` 记录阶段 55；仅改员工客户端相关目录链路 | 初始工作区仅规格书未跟踪；真实主入口为来源驱动生成，知识库 patch 函数在当前生产链无调用 |
| 2. 回归测试 | 旧“六个三级叶子拒绝”断言 | 改为六个独立叶子、混合深度、单分支及保护反例；保留深度/权限负例 | 测试先在旧代码出现 3 个预期失败，修改后对应项通过；初始聚焦基线 38/40，另两条旧 Word 门禁失败 |
| 3. 统一提示词 | 初次生成、修复、字数调整、参考知识库、正文补目录 | 共用无副作用的自然拆分文本；各入口仍保持原 JSON 全量或 patch 形状 | 实际请求和构造器测试检查无“第六项强制归类”“单分支补第二项”等旧指令；知识库 patch 仅单测，非生产端到端 |
| 4. 校验与修复 | 来源校验和两条增量补丁校验 | 移除数量硬错误；保留重点五级、最大深度、来源顺序、人工及已有内容保护；拒绝重复 ID、非法 children | 合成树、六叶子、单分支、非重点五级、重复身份和非法类型测试通过；字数调整次数/用户目标未改 |
| 5. 正文与导出 | 三级/四级/五级混合叶子、人工祖先、历史父级正文 | 正文、配图与 UI 重生成入口继承人工保护；祖先说明进入章节上下文；普通/章框 Word 保留历史父级正文 | 七叶子目标、时限/图示说明、配图保护及 DOCX XML 标题/正文一次性检查通过；真实 Word 应用打开未执行 |
| 6. 只读审查与留痕 | 候选目录、来源骨架、项目概述、现有技术要求和评分组 | 审查仅返回旧 Schema 的 passed/warning/unavailable，不回写结构；任务日志记录规则摘要及各层叶子/分支统计，不含全文材料 | warning 不回写、审查上下文包含要求的模拟测试通过；模型审查不可用时保留 unavailable |
| 7. 效果验证 | 合成数据、本地开发页、用户规格所述真实样本 | 不在用户现有项目上破坏性生成；不把离线测试当真实模型效果 | `npm.cmd run build` 通过；全量 Node 测试 235/247，12 条未通过见下；Playwright 仅到员工登录页，未进入目录；真实 Word/PDF 样本未在工作区找到，故未做真实模型 A/B |
| 8. 文档与审查 | CHG-002、当前差异与测试证据 | 标记旧数量门禁被本规格替代；保留历史与未冲突边界 | 独立只读 Reviewer 复核后未发现新的本轮代码阻断；`git diff --check` 通过 |

## 既存门禁断言与当前人工编制规则

- `technicalPlanExport.test.cjs` 两条断言仍要求 `pending`/`needs-manual-input` 或旧“缺证明材料风险”状态硬阻止 Word 导出；当前产品以人工节点及其正文现状决定编制与导出，不再通过这些旧 `response_status`/`response_mode` 门禁。最初基线运行已见这两项失败。本轮没有为了测试全绿恢复旧门禁。
- `contentIllustrationGeneration.protection.test.cjs` 两条断言以旧 `response_mode='locked-commitment'` 判定不可配图；当前有效的人工保护是 `manual_input_required`，并在本轮补上祖先继承。旧字段门禁仍失败；新人工祖先与图片/附件引用保护测试通过。
- 全量测试其余 8 条失败分布于招标解析、旧章节合同、技术方案存储和 UI 静态源码断言。本轮全量复跑失败名单未变化；未逐条在干净 HEAD 上隔离，不能声称它们都已证实为基线故障。全量套件不能报告为通过。

## 验证边界

可达的知识库参考目录已进入首次生成请求；旧 `enhanceOutlineWithKnowledgeAdditions()` 当前无生产调用，因此其提示词和 patch 单测不等于知识库增补端到端通过。当前产品也未发现独立“仅目录导出”入口，未执行该项专用 UI 验证。浏览器冒烟看到登录页，但无授权账号/不接触真实用户数据，目录编辑界面及 Electron 进程联调未执行。用户规格提到的 Word 问题样本和同源招标 PDF 不在当前工作区，真实模型配置、预算和 A/B 效果未核实；不能宣称结构自然度或节省工时已获实测。没有打开历史真实项目，也没有运行破坏性全量重新生成；旧项目不迁移是本轮代码边界，尚非实项目验收结论。现有全量重生成的副本/恢复能力未经本轮实机核验；后续若用真实项目验证，应先建立并核验可恢复副本。
