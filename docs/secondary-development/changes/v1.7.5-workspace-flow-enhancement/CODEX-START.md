# v1.7.5：发给 Codex 的实施指令

先将主规格与配套目录放入仓库，再粘贴以下内容：

```text
先阅读当前仓库根目录及修改范围内的AGENTS.md、开发说明.md，再完整读取：
docs/v1.7.5-spec.md
docs/secondary-development/changes/v1.7.5-workspace-flow-enhancement/HANDOFF.md
docs/secondary-development/changes/v1.7.5-workspace-flow-enhancement/TEST-CASES.md
docs/secondary-development/changes/v1.7.5-workspace-flow-enhancement/REVISION-NOTES.md

本轮客户端目标版本明确为v1.7.5（版本字段1.7.5）。前面三轮正文写作、目录自然拆分和项目理解已实现并合并，只做新规格中的增量，不重做旧任务、不覆盖两份v1.7.4规格。
旧workspace-flow-enhancement/spec.md关于“保持原版本”的约定已被本版替代。

按主规格第13节T01—T10实施，每步按输入、操作、输出和约束交付。先核对本地分支、提交、未提交改动、真实调用链、原右上角解析动作以及当前客户端版本，不回退到文档里的旧审查提交。
按第10.1节同步客户端版本源、相关锁文件和既有版本显示，在T09构建前完成。若本地已是1.7.5则不重复升版；若已更高或存在用户未提交版本调整，先报告并保留，不降级、不覆盖。

冻结范围：
1. 项目列表在标书生成内部、上传文件之前独立显示；点击项目名称进入。没有项目状态列，操作仅红色删除，确认后只清目标项目。
2. 不新增主动暂停、暂停后切换或跨项目并行。任务完成或故障真正停止后可换项目；打开项目不自动执行AI。
3. 原Step01改为精简文件列表。一个“添加文件”支持一次多选与分批追加；无转换状态列、转换按钮、资料集卡片或大Markdown阅读区，不合并解析结果面板。
4. “开始招标文档解析”保持右上角，复用原解析设置、标段选择与任务逻辑，不移到右下角。
5. 原解析结果查看页只加Word导出；直接导已存结果，不用AI写摘要，不按示例图片重做原结果主体。
6. 目录大纲整体导出Word；升降整棵子树只在正文生成前使用，不做正文、图片或附件迁移，不跳过全局事实。
7. HTML只改后台自动16:9／4:3和现有图种表达，保留原计划、存储、本地渲染与插入；无新UI、无比例选择器、无新图表库或Skill。
8. 一级Tab、现有Logo和窗口外壳不改，项目数据真正隔离；userData、授权、管理端版本和发布系统保持原状。

完整阅读Handoff差异裁决与资源清单。四张参考图原样复用，图中示例版本、日期、格式和额外控件不能成为生产需求。CSS只是作用域布局参考，不能覆盖全局主题。

运行原60组功能及新增V01—V08版本专项对应的验证；没有环境执行的项目明确记录，不宣称通过。
使用合成项目或授权副本，不删除用户真实项目。交付代码、版本修改清单、迁移和配置说明、四页实机截图、两类DOCX、两种PNG比例样例、实施记录及分层测试报告。

没有真实安全、权限或规格冲突就继续实施，不只输出计划。不要擅自推送、合并、打标签、创建Release或发布安装包。
```

**一句话总结：**按v1.7.5完成已经确认的轻量工作区、流程导出、正文前目录调整及后台横版配图，保留原UI边界和前三轮成果。
