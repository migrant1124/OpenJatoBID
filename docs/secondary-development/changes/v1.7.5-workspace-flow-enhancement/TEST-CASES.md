# v1.7.5 验收与回归测试用例

配套：[v1.7.5-spec.md](../../../v1.7.5-spec.md) / [HANDOFF.md](HANDOFF.md)。本文件为待执行用例，不是已经通过的测试报告。

**目标软件版本：v1.7.5；文档修订：1.1。共68组：保留原60组功能测试，追加V01—V08版本专项。全部为待执行要求，不是通过报告。**

测试使用两个可丢弃的合成项目A/B；有真实样本时先获得授权并建立可恢复副本。本文不允许Codex直接删除用户真实项目。

## 1. 测试分层

| 层级 | 验证对象 | 不能替代的验收 |
| --- | --- | --- |
| L1 单元与纯函数 | 比例选择、树操作、编号、去重、导出内容适配 | 不能代替真实文件与UI |
| L2 Store／服务／IPC | 项目隔离、事务、任务绑定、删除、写回及迁移 | 不能代替Electron真实登录后操作 |
| L3 UI交互 | 四页流程、按钮位置、禁用、焦点、提示、窗口缩放 | 不能只看源码中是否有按钮文案 |
| L4 文件结果 | DOCX内部结构与超链接、实际PNG尺寸 | 不能代替Word/WPS打开和肉眼阅读 |
| L5 实机与真实模型 | 项目轮换、长中文、普通／章框Word、实际生成效果 | 无凭据时明确未执行，不用mock冒充 |

## 2. 项目工作区与删除：12组

| ID | 输入／操作 | 预期结果 | 建议层级 |
| --- | --- | --- | --- |
| W01 | 在无项目状态新建“项目A” | 产生稳定ID和工作区，进入资料页；不调用AI；一级导航不变 | L2/L3 |
| W02 | 新建同名的第二个项目 | 不覆盖A；两项目有不同内部身份 | L2 |
| W03 | 为A/B分别保存不同文件、解析、目录和项目理解引用，反复切换 | 每次只显示相应项目；引用、图片与导出记录不串 | L2/L5 |
| W04 | A完成解析，未进入下一步，转到B，再打开A | 恢复已保存解析结果，不自动生成目录，不出现暂停流程 | L3/L5 |
| W05 | A有运行中AI、Agent、导入、渲染或导出请求时尝试切项目 | 前端和Main均阻止；空闲Agent进程不误阻止 | L2/L3 |
| W06 | A遇故障，有部分完整结果，其他请求仍在清理 | 清理完成前不能切；全部停止后可切；A结果和错误保留 | L2/L5 |
| W07 | 关闭后重开，原任务未正常结束 | 不误判成功，不自动开跑；保留完整结果，按原能力重试 | L2/L5 |
| W08 | 在P01点击删除后选择取消／ESC | 没有任何项目或文件删除；焦点正确返回 | L2/L3 |
| W09 | 确认删除A，B引用公共模板和知识库，A原件在桌面、外部另存Word也存在 | 清理A受管内容；B、公共库、桌面原件、外部导出不受影响 | L2/L5 |
| W10 | 删除目标仍在运行、或删除过程中被系统锁文件 | 运行中拒绝；锁文件明确失败，不报成功；可安全重试 | L2/L5 |
| W11 | 删除后模拟A迟到的异步回调；并传入越界路径／符号链接 | 不复活A、不写B、不删工作区以外数据 | L2 |
| W12 | 旧单项目迁移、重复启动，以及模拟迁移中断 | 原成果与图片/证据保留，只登记一次；失败可回滚，公共库不被归属A | L2/L5 |

## 3. 多文件管理与流程：12组

| ID | 输入／操作 | 预期结果 | 建议层级 |
| --- | --- | --- | --- |
| F01 | 一个“添加文件”动作选择三份支持文件 | 全部归当前项目；没有另一个批量添加入口 | L2/L3 |
| F02 | 首批A1/A2已保存，第二次添加A3 | A1/A2仍在，只追加A3；不会清空来源集合 | L2/L5 |
| F03 | 重复加入字节完全相同的文件，名称可相同或不同 | 提示重复、跳过；不重复分析，不产生无效资料修订 | L1/L2 |
| F04 | 同名但内容不同，分别选择保留两份与替换 | 文件身份不冲突；替换明确指向旧文件，不按名字静默覆盖 | L2/L3 |
| F05 | 批量两份成功一份失败；或替换新文件解析失败 | 成功文件保留，失败集中提示；替换失败旧文件不丢 | L2/L5 |
| F06 | 移除A的一份托管文件 | 其他文件和外部原件不动，资料修订更新 | L2 |
| F07 | 检查P02表头、按钮和卡片 | 只有文件名称/类型/上传时间/操作；无转换状态列、转换按钮、资料集状态卡 | L3 |
| F08 | 分别在1425×912与1366×768查看并点击解析主动作 | 入口处在标题区域右上方，不在底部；沿用原设置和范围确认链 | L3/L5 |
| F09 | 上传时尚未准备完成就启动解析，或在运行中替换输入 | 不使用半输入；原busy/错误反馈有效，不恢复状态列 | L2/L3 |
| F10 | 已有目录正文时追加澄清，再不执行重新分析 | 旧成果保留，解析待复核提示正确，不自动AI重跑 | L2/L5 |
| F11 | 两份来源有同号条款且冲突；含通用条款＋不同标段附件 | 来源边界不混、选定范围不丢通用条款；不按上传时间覆盖 | L1/L2/L5 |
| F12 | 当前解析器不支持图片或某表格格式，图片示例中却有该格式 | 实际按解析器拒绝／提示；说明不宣传未支持格式，不新建解析器 | L2/L3 |

## 4. 解析报告Word导出：8组

| ID | 输入／操作 | 预期结果 | 建议层级 |
| --- | --- | --- | --- |
| R01 | 只有解析结果，没有目录和正文时导出 | 成功生成可编辑报告，不受最终正文门禁影响 | L2/L4 |
| R02 | 当前只展开一个结果项，实际已选多个解析项 | 输出全部实际选中项及其状态，顺序与定义一致 | L1/L4 |
| R03 | 含Markdown表格、嵌套JSON、0/false、空数组和缺字段 | 表格可编辑，中文字段正确，不把不同含义一律丢为空白 | L1/L4/L5 |
| R04 | 部分失败后停止；或资料更新而分析版本旧 | 明确部分／过期范围，文件清单与分析快照相配，不伪装完整 | L2/L4 |
| R05 | 模型服务计数器拦截所有AI调用后执行导出 | 不调用AI仍可导出，不重写文本 | L2 |
| R06 | 取消保存／路径无权限／文件被占用 | 不改变解析结果；取消不是失败，其他错误如实反馈 | L2/L3/L5 |
| R07 | 对照修改前的现有解析页 | 仅增加Word导出，不改原结果结构、查看、解析设置和重试 | L3 |
| R08 | 用Word/WPS打开长表、来源链接及多页报告 | 内容完整、表头合理、链接可用、无溢出版心；记录真实软件与版本 | L5 |

## 5. 目录层级及大纲导出：10组

| ID | 输入／操作 | 预期结果 | 建议层级 |
| --- | --- | --- | --- |
| O01 | 正文未开始，选中三级节点及两层后代，升一级 | 整个子树上升，放原父后，内容说明与属性保留，统一编号 | L1/L3 |
| O02 | 正文未开始，选中第二个同级，降一级 | 成为前同级最后子节点，后代完整；当前选择不串 | L1/L3 |
| O03 | 首个同级降级、根节点升级、移动后超最大深度 | 精确禁用／拒绝，不自动造父标题或调整其他节点凑结构 | L1/L3 |
| O04 | 来源锁定、人工祖先、非重点五级、混合深度样例 | 保留原约束与自然拆分，不恢复“至少两个/最多五个”限制 | L1/L2 |
| O05 | 上移下移、连续升降后保存，再取消另一批未保存动作 | 保存版本准确，取消恢复草稿前版本，编号正确 | L1/L3 |
| O06 | 正文曾启动或扩写导入了已有正文，再访问目录 | 本轮升降级不启用，不清正文/图片/附件，不做迁移 | L2/L3 |
| O07 | 层级变化导致路径式ID变动，含项目理解定位和来源锚点 | 目录阶段必要引用跟随一致，无孤儿节点或旧ID误绑 | L2 |
| O08 | 折叠大部分目录后导出，分别开关附说明 | 所有节点输出；说明开关有效；无正文、图和内部标签 | L1/L4 |
| O09 | 存在未保存修改时导出，或无目录时点击 | 先保存或取消，无旧新不一致；空目录禁用；导出不触发AI | L2/L3 |
| O10 | Word/WPS打开混合三级/四级/五级大纲；继续下一步骤 | 标题层级、编号和导航正确，无假最终页码；原全局事实步骤未被跳过 | L4/L5 |

## 6. HTML默认横版与图种：10组

| ID | 输入／操作 | 预期结果 | 建议层级 |
| --- | --- | --- | --- |
| H01 | 流程/步骤/时间轴的代码值和中文标签 | 默认16:9，输出2560×1440，归一化一致 | L1/L4 |
| H02 | 组织/架构/层级/矩阵以及未知类型 | 对应默认4:3或合法已选横版，输出2560×1920，不能纵向长图 | L1/L4 |
| H03 | 同输入分别走普通模型、Agent、布局修复和恢复 | 同一目标宽高贯通；不回到1240宽高度自适应的旧新图路径 | L2/L4 |
| H04 | 文字或节点溢出目标画布，使用overflow隐藏伪装 | 布局质检不通过；不能靠裁切隐藏、压扁和缩小文字过关 | L2/L4/L5 |
| H05 | 首选比例布局失败，允许在原修复预算内试另一比例 | 在固定预算内执行并保存最终比例，不无限重试或静默多图 | L2 |
| H06 | 有分支流程、三层组织、有序时间、分层架构 | 真实关系存在；不能只输出无关联卡片或把统计图冒充表格 | L1/L4/L5 |
| H07 | 正文未提供日期、人员或数值 | 不虚构数值与事实；保持与当前文本一致 | L1/L5 |
| H08 | 修改比例规则后打开历史项目；调用旧任意HTML/其他图种渲染 | 不自动重做旧图，不全局改旧渲染尺寸；新生成走新规则 | L2/L4 |
| H09 | 检查全部路由、设置页及配图操作入口 | 不存在本轮新增HTML控制页、比例选择、图种手控卡片、新库或Skill | L2/L3 |
| H10 | PNG插入普通／章框Word并实机打开；生成失败 | 比例不变、文字可读；失败保留正文；原隔离及默认5/最高20并发保留 | L2/L4/L5 |

## 7. 回归、设计与交付：8组

| ID | 输入／操作 | 预期结果 | 建议层级 |
| --- | --- | --- | --- |
| X01 | 对照用户真实原截图和当前AppShell | 一级Tab、Logo、侧栏、原生窗口不重设计，无新增中文品牌大标题或全英文界面 | L3 |
| X02 | 检查页序：项目列表→资料→结果→目录→全局事实→正文 | 资料与结果不合并，项目列表不在侧栏重复，没有独立转换阅读步骤 | L3 |
| X03 | 两条入口分别运行：新编技术方案／已有方案扩写 | 原方案独立，复用流程不回归；不把原方案混入招标资料 | L2/L5 |
| X04 | 前三轮既有测试，特别项目理解证据引用、人工保护、目录自然拆分 | 不回退；基线失败如实区分，不靠弱化断言制造通过 | L1/L2 |
| X05 | 检查请求日志和遥测，在增删/导出/出错后观察 | 不外发原文、名称、路径、密钥等敏感数据；不放宽Agent/渲染权限 | L2 |
| X06 | 中文超长项目名、文件名，Windows125%/150%，1366×768与1920×1080 | 按钮可达、表格内部滚动、无重叠；解析主按钮仍在上方右侧 | L3/L5 |
| X07 | 保存中断、磁盘不足、导出中删除、并发点击新建/切换 | 不丢既有结果、不串项目、不重复创建、不写已删除项目 | L2/L5 |
| X08 | 审核交付资料与实际执行记录 | 有代码/迁移说明/四页截图/两类DOCX/两比例PNG及分层报告；未执行项明确，无擅自推送发布 | 审查 |

## 8. v1.7.5 版本与升级专项：8组

| ID | 输入／操作 | 预期结果 | 建议层级 |
| --- | --- | --- | --- |
| V01 | 基线客户端为1.7.4；核对实际版本源和锁文件并实施版本同步 | 本客户端根包版本一致为1.7.5；第三方依赖版本与不相关锁条目不被升级 | L1/L2 |
| V02 | 以1.7.5开发运行，读取Main版本、bridge返回、已有登录和关于页 | 使用同一真实版本源，显示一致；没有为了改号新增页面硬编码 | L2/L3/L5 |
| V03 | 按既有本地构建流程打包，检查候选包元数据并在授权测试环境启动 | 候选包及运行时为1.7.5，命名模板不变；未打包／安装则分项写未验收，不替代成已通过 | L4/L5 |
| V04 | 当前已是1.7.5、当前高于1.7.5、版本文件存在未提交调整三种情形 | 同版本不再升；更高版本不降级；未提交调整不覆盖；冲突明确报告 | L2/审查 |
| V05 | 从v1.7.4测试副本升级到1.7.5，再次启动 | 产品身份、userData和授权位置不变；旧项目只迁移一次，正文、图片、引用与模型配置保留 | L2/L5 |
| V06 | 检查SQLite迁移、资料／目录修订、研究和图片计划版本 | 软件号与这些版本独立；迁移不倒退，既有编号不被全局替换为1.7.5 | L1/L2 |
| V07 | 审阅主规格、Handoff、Codex指令、资源清单和原始图片 | 执行入口为docs/v1.7.5-spec.md；本轮目标一致；本地链接有效；五张源图片哈希不变，旧v1.7.4规格未覆盖 | 审查 |
| V08 | 检查版本修改清单、Git及发布动作记录、管理端／Analytics差异 | 只同步授权的客户端版本；不因升版改其他子系统，不擅自推送、打标签、创建Release或更新线上latest.json | 审查 |

V03中的候选安装包验证不授予远端发布权限。V04必须在合成工作区或测试副本中验证，不实际改写用户的高版本仓库。

## 9. 报告模板

| 项目 | 填写要求 |
| --- | --- |
| 软件版本／文档修订 | 目标软件v1.7.5，主规格修订1.1；填写实际读取的开发／安装版本，不能只复述目标值 |
| 实施提交／本地状态 | 实际提交、分支、未提交改动；不用本SPEC的审计提交代替 |
| 测试环境 | OS、Electron/Node、依赖安装情况、是否授权登录、显示缩放 |
| 基线 | 改动前测试结果和失败项，是否在干净副本隔离证实 |
| 新增用例 | 用例ID、运行方式、输入夹具、结果、证据位置 |
| 真实文件 | DOCX与PNG检查方式，是否由Word/WPS实际打开 |
| 模型测试 | 模型/配置、使用授权、输入范围、样例结果；没有则写未执行 |
| 未完成项 | 原因、风险、需要的用户动作，不能一概写“基本完成” |
| 回滚与迁移 | 副本备份还原是否实际验证，删除是否影响共享数据 |
| 发布 | 本轮默认未推送、未合并、未打标签、未发布；有另行授权则如实注明 |

UI不存在项的检查需限定页面：P02不显示转换状态，但后台仍允许必要转换状态；P01不显示项目状态，但任务层仍必须有运行状态。不得为了消除文案删掉必要后台保护。
