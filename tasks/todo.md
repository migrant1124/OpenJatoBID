# OpenJatobid 二次开发任务清单

> 当前状态：既有第二轮仍等待 T30/CP6 实体双机验收；阶段 7 等待 CP13 最终验收；阶段 8 已按确认的发布优化方案进入 Build。

## 决策门

- [x] 确认 A1：管理端采用纯 Electron 托盘模式，不保证 Windows 用户注销后继续服务。
- [x] 确认 A2：产品名“Jato AI BID 管理端”，目录 `management/`，appId `com.jiatu.aibid.management`。
- [x] 确认 A3：服务器输入兼容 `IP`/`IP:端口`，默认端口 `47821`。
- [x] 确认 A4：管理端首次生成 ECDSA P-256 密钥，客户端首次授权绑定公钥。
- [x] 确认 A5：设备标识沿用系统机器标识、网卡信息和持久客户端 ID 的组合。
- [x] 确认 A6：旧公共/离线授权失效并重新申请；保留本地 `analytics_client_id`。
- [x] 确认 A7：续期从操作时起延长 1 年；拒绝不填写原因。
- [x] 确认 A8：在线口径 10 分钟；离线埋点最多 30 天或 10,000 条。
- [x] 确认 A9：统计保留 24 个月，支持管理员手动清理。
- [x] 确认 A10：管理端复用同一套 Ant v6 视觉令牌，但工程完全独立。
- [x] 确认 A11：单一内置管理员凭据，首次登录强制改密，登录后主动改密；不提供注册、SMTP、忘记密码或自助恢复。
- [x] 确认 A12：不新增独立心跳，用登录、校验和业务埋点刷新在线状态。
- [x] 确认 A13：内置凭据由被 Git 忽略的私有构建配置注入，公开仓库不保存明文密码。
- [x] 确认 A14：同一局域网只部署一个管理端，外部运行与安装包保管采用公司负责人负责的人防制度。
- [x] 批准 PRD 0.5 并允许进入技术规划阶段。
- [x] 批准 PRD 0.5 管理员认证修订计划并允许进入编码阶段。

## 阶段 0：决策与基线

- [x] T00 确认计划，关闭 PRD 开放项并建立架构 ADR。
- [x] T01 建立关键页面视觉基线和 Ant v6 令牌映射。

## 阶段 1：客户端内容与视觉基础

- [x] T02 修改模型配置名称/链接和隐私声明。
- [x] T03 实现全局视觉令牌与主壳样式。
- [x] T04 统一共享弹窗、表单、提示和 Markdown 视觉。
- [x] CP2 验收：客户端视觉基础可演示。

## 阶段 2：独立管理端基础

- [x] T05 创建 `management/` 独立工程与构建配置。
- [x] T06 实现窗口、preload、托盘、单实例和自启动。
- [x] T07 实现本地 SQLite schema 与迁移。
- [x] T08/T09 完成原 SMTP 管理员认证方案；该方案已被 PRD 0.5 否决，不再作为目标状态。
- [x] CP2 验收：管理端首次设置与托盘生命周期可演示。

## 阶段 3：授权闭环

- [x] T10 冻结 LAN API 契约并实现 HTTP 服务骨架。
- [x] T11 实现授权申请、审批、拒绝、撤销、续期和签名服务。
- [x] T12 实现管理端授权管理界面。
- [x] T13 建立客户端局域网配置与授权 IPC 契约。
- [x] T14 重写客户端授权服务和本地离线校验。
- [x] T15 实现启动登录页和左下角授权申请弹窗。
- [x] T16 实现启动/联网/休眠恢复校验与状态提示。
- [x] T17 清除旧公共授权与离线激活界面/接口。
- [x] CP3 验收：申请—审批—登录—离线—撤销完整闭环通过。

## 阶段 4：局域网统计

- [x] T18 建立客户端 Main 侧统一埋点服务与有界离线队列。
- [x] T19 迁移 Renderer 埋点到统一 IPC。
- [x] T20 迁移 AI 与 Agent Runtime 埋点到统一服务。
- [x] T21 实现管理端埋点接收、去重、聚合与清理。
- [x] T22 实现统计总览、客户端与 IP 页面。
- [x] T23 实现 AI、配置、Agent 与最新事件页面。
- [x] T24 验证公共 `/track`/授权完全切断且公共内容服务保留。
- [x] CP4 验收：局域网统计闭环和网络边界通过。

## 阶段 5：完整 UI 换肤

- [x] T25 迁移技术方案与工作区页面视觉。
- [x] T26 迁移查重与废标检查页面视觉。
- [x] T27 迁移知识库、模板、资源、设置与开发者页面视觉。
- [x] T28 完成响应式、可访问性和固定视口视觉回归。
- [x] CP5 验收：全部主页面 UI 换肤完成且流程/布局无回归。

## 阶段 6：集成与交付

- [x] T29 完成管理端 Windows 打包、解包运行和隔离数据目录验证；实体安装/升级纳入 T30 现场验收。
- [ ] ~~T30 完成 Windows 管理端 + Windows/macOS 客户端双机端到端验收。~~
- [x] T31 完成 PRD 0.4 全量构建、CJS 检查、依赖审计、打包、代码审查和交付文档；PRD 0.5 由 T36 重新出具证据。
- [ ] ~~CP6 验收：PRD 验收项、测试报告、部署文档和安装包全部完成。~~

## 阶段 6A：PRD 0.5 管理员认证修订

- [x] T32 实现私有构建凭据注入，确保仓库和生成模块不保存明文密码。
- [x] T33 实现内置凭据接管、旧 SMTP 认证迁移和负责人密码状态。
- [x] T34 实现“初始登录—强制改密—服务器设置—管理界面”纵向流程。
- [x] T35 实现登录后主动改密，并从依赖、服务、IPC、界面和数据库设置中全量移除 SMTP/忘记密码。
- [x] T36 更新文档、运行管理端全量验证并重新生成 Windows 安装包和哈希。
- [x] CP7 验收：管理员认证修订和管理端新安装包通过，允许进入实体双机最终验收。

## 阶段 6B：第三轮调试、关于页与图标优化

- [x] T37 实现两端浏览器调试入口和真实 Electron 调试命令。
- [x] T38 实现关于页登录账号卡片与当前会话退出。
- [x] T39 实现隐私声明宽屏四列、中屏两列、小屏单列布局。
- [x] T40 修复客户端 Windows 应用身份和管理端托盘图标。
- [x] T41 完成两端构建、浏览器与 Electron 运行验收。

## 阶段 6C：Upstream 选定功能同步

- [x] T42 同步 Agent 全文图片编排/生成链路，并将工作区 SQLite 升级到 v17。
- [x] T43 同步正文生成的 AI、HTML 配图配置与示例图；隐藏 Mermaid 配置入口，默认关闭并兼容历史配置。
- [x] T44 删除“龙猫”可选文本模型入口，同时保留历史配置加载与显示兼容。
- [x] T45 让废标检查的小 Markdown 卡片不再显示“全屏”。
- [x] T46 完成 CJS 语法、客户端构建、SQLite v17 迁移、配置兼容和关键运行链路验证。

## 最终证据

- [ ] ~~客户端 `npm ci`、测试、构建和 `dist:win` 通过；`npm audit` 已执行，但既有依赖仍有 14 项漏洞（含 `xlsx` 无可用修复），见最终测试报告。~~
- [x] 管理端 `npm ci && npm test && npm run build && npm audit && npm run dist:win` 通过。
- [x] 所有改动的 Electron CJS 文件通过 `node --check`。
- [x] 授权状态机、3 台上限、1 年期、30 天离线、撤销、签名均有自动化测试。
- [x] 埋点离线队列、补发、事件去重和统计聚合均有自动化测试。
- [ ] ~~运行时抓包证明授权和埋点只访问局域网管理端。~~
- [x] 公告、资源、更新检查代码路径保留；现场联网可用性纳入 T30。
- [ ] ~~两个软件已完成独立打包和解包运行；实体安装、升级和卸载纳入 T30。~~
- [x] 最终测试报告无未解释失败，代码审查无本轮新增 P0/P1 问题。
- [x] 仓库中不存在测试数据库、私钥、SMTP 授权码或其他凭据。
- [x] 私有初始凭据文件和生成摘要模块均被 Git 忽略，源码、文档、构建日志和打包清单不包含明文初始密码。
- [x] 管理端不存在 SMTP、恢复邮箱、临时密码、忘记密码和 `nodemailer` 产品能力；仅迁移清理与回归测试保留旧字段名。
- [x] 初始用户名/密码登录后强制改密且不允许复用初始密码；主动改密要求当前密码，成功后旧密码失效。
- [x] 旧 SMTP 认证数据库升级后保留员工、设备、授权、统计和签名身份，仅重置管理员接管状态并删除 SMTP 配置。
- [x] PRD 0.5 管理端测试、构建、审计、CJS 检查、Windows 打包和解包运行全部通过。

## 阶段 7：招标解析、格式驱动目录与受控写作

> T47—T69 和 CP8—CP11 保留为历史实施与验收记录，不再代表当前目标行为。当前目标以阶段 7F、CHG-001 和 `tasks/plan.md` 第 13 节为准。

### 决策门

- [x] CP8 批准 `docs/secondary-development/adr/format-driven-technical-plan.md`、`docs/secondary-development/api/technical-plan-format-contract-v1.md`、`docs/secondary-development/design/format-driven-technical-plan-ui.md` 与 `tasks/plan.md` 第 12 节，允许进入 Build。
- [x] 确认 v18 使用解析项 `normalized_hash`、`format_constraints_json`、`response_state_json` 和 `technical_plan_response_templates`。
- [x] 确认格式任务读取全部原始招标文件的来源锚点；“采购与报价”读取当前投标范围工作副本。
- [x] 确认无明确格式自动使用全局 technical/none profile；显式格式 profile 零个或最高分并列多个匹配时阻断并要求用户选择，禁止猜测或评分回退。
- [x] 确认已有方案扩写也服从格式优先，`original-only` 仅用于无明确格式。
- [x] 确认未确认模板可进入 Step03，但阻止对应写作和导出。
- [x] 确认缺少强制证明材料可在显式风险确认后导出，其他结构完整性问题硬阻断。
- [x] 确认证明材料 v1 只生成 Markdown 索引和知识条目引用，不嵌入原附件。

### Phase A：解析层

- [x] T47 冻结运行时契约并完成 SQLite v18 基础。
- [x] T48 建立 Main 单一任务目录与完成门禁；原 19 项/7 必选目录最终由 T65 调整为 18 项/7 必选。
- [x] T49 完成格式结构化解析、模板注册和 Step02 待核对展示纵向切片；来源链路最终由 T65 重构。
- [x] T50 完成原独立“报价要求”解析与技术正文隔离；该任务最终由 T65 退役并合入“采购与报价”。
- [x] T51 完成旧 5 项工作区补跑、步骤恢复与解析结果失效闭环。
- [ ] ~~CP8A 验收：Phase A 自动化、Electron smoke、构建和 Step02 运行证据通过。~~

### Phase B：格式驱动目录

- [x] T52 完成 profile 解析与格式优先目录纵向切片。
- [x] T53 完成格式门禁、评分门禁和受控 Agent/知识库/原方案修复。
- [x] T54 完成目录 Store 锁定、编辑器状态和三类编号预览。
- [ ] ~~CP9 验收：Step02→Step03 格式骨架、评分覆盖、编号和锁定证据通过。~~

### Phase C：受控写作

- [x] T55 完成固定模板服务和固定承诺函纵向切片。
- [x] T56 完成固定 Markdown 表格纵向切片。
- [x] T57 完成 response_mode 调度、证明材料、明确无内容和合规状态。
- [x] T58 封堵普通保存、扩写、Agent 修复、表格清理、补目录、配图和内部 Store 写入绕过，并完成缓存失效。
- [ ] ~~CP10 验收：六种 response mode 与两个技术方案入口的自动化和真实页面证据通过。~~

### Phase D：导出、回归与审查~~

- [x] T59 完成权威 Store 导出、编号策略、固定模板复核和缺证据风险确认。
- [x] T60 完成自动化、v17→v18 迁移、Electron 运行、DOCX 检查和测试报告。
- [ ] ~~T61 使用建行珠海、国网湖北、南网超高压、四川烟草四个真实样本完成端到端验收和独立 Review。~~
- [ ] ~~CP11 最终验收：冻结需求 DoD、四样本、构建、运行、导出和独立 Review 全部通过。~~

### Phase E：1.2.0 现场回归修复

- [x] T62 完成“投标文件格式要求/报价要求”首轮 JSON 链路整改：补齐完整字段契约，改为一次完整提取、确定性二次校验和失败后仅修复当前结果；结构化首轮禁止整项语义重跑。
- [x] T63 重建 NSIS、ZIP、MSI 客户端 Windows 产物，确认三者内置同一份 SQLite schema v18 `app.asar`，并用隔离的 schema v18 用户目录完成启动门禁验证。
- [x] T64 完成逐字摘录与来源行号重算的第二轮修订；现场回归仍失败，确认单物理行 HTML 表格的可见文本无法反向匹配原始标签结构，该方案由 T65 替代。
- [x] T65 按批准方案完成来源锚点与固定模板二阶段解析、18 项/7 关键项目录、“采购与报价”、格式要求 UI 名称及配置 Dialog 叠字修复；61 项聚焦测试、相关 CJS 语法检查和客户端构建通过，两轮只读复审无 P0/P1/P2，按用户安排未打包且真实模型端到端解析由用户现场测试。
- [x] T66 修复格式要求普通目录节点误用固定模板连续锚点约束：目录行与远处同名正文标题由 Main 按编号/标题确定性收敛到唯一目录锚点，歧义候选继续阻断，固定模板仍保持严格连续；清理纯 HTML 关闭标签伪锚点，并补充低敏格式解析调试日志、脱敏回归用例和国网湖北现场样本锚点只读复算。33 项聚焦测试、相关 CJS 语法检查和客户端构建通过；未打包，真实模型端到端解析继续由用户现场测试。
- [x] T67 对照 `upstream-main` 原“响应文件要求”链路收敛格式来源职责：普通目录节点由 Main 基于 `source_number + source_title` 在全量真实锚点中确定性匹配；分散规则来源按真实顺序拆分；仅固定表格补齐同表漏选行；未知锚点、跨表格和跨普通正文来源继续阻断。最新现场原始响应离线重放已越过全部第一阶段来源校验并进入模板编译；CJS 检查、34 项聚焦测试、客户端构建和两轮只读复审通过，未打包、未调用真实模型完成第二阶段。
- [x] T68 修复模型将完整 `source-anchor-` ID 缩写为裸 20 位哈希后被误报未知来源锚点：仅当同一不可变 catalog 存在唯一完整 ID 时确定性还原，目录、规则和固定模板统一使用该规则，真正未知、带不可见字符或畸形 ID 继续阻断。现场最新原始响应基于同一 1308 项 catalog 离线重放已越过第一阶段锚点校验并进入模板编译；49 项聚焦测试、相关 CJS 语法检查和客户端构建通过，未调用真实模型、未打包。
- [x] T69 修复格式模型重复引用同一锚点及固定表格混入上一表格离群行：同一来源引用仅折叠完全相同的 canonical ID；固定表格仅在恰好一个连续候选组包含至少两行真实表格时采用该组，多个表格候选、承诺函跨段、未知锚点和跨文件引用继续阻断。最新现场原始响应使用同一 1308 项 catalog 原样离线重放已进入固定模板编译；49 项聚焦测试、相关 CJS 语法检查和客户端构建通过，未调用真实模型、未打包。

### Phase F：CHG-001 Markdown 格式要求与目录来源重基线

- [x] T70 完成需求变更记录、ADR、简化契约、UI 基线和 `tasks/plan.md` 第 13 节；确认恢复 `responseFileRequirements`、两类一级目录来源、无格式/无知识库阻断及人工填写节点。
- [x] CP12 已由用户批准 `docs/secondary-development/changes/format-requirements-simplification-change.md`、修订后的 ADR/API/UI 基线与 `tasks/plan.md` 第 13 节，允许进入 Build。
- [x] T71 恢复 `responseFileRequirements` Markdown“格式要求”关键项和固定状态首行，格式解析仅走通用 Markdown 请求；格式专属 JSON、锚点、模板编译和专属结果 UI 已退役。
- [x] T72 实现“明确格式→格式要求一级目录；无明确格式→仅用所选知识库目录一级目录”，多知识库单次综合，未选知识库零目录调用阻断，技术评分项和原方案只允许补充二级及以下。
- [x] T73 实现 `manual_input_required`、普通 Markdown 人工填写、正文/扩写/自动处理跳过和空内容导出阻断；旧 v18 固定模板节点按人工 Markdown 兼容读取。
- [x] T74 删除无生产引用的 AnchorCatalog、结构化 format Schema、profile/固定模板编译、专用 IPC/UI 和旧回放测试；保留 SQLite v18 表/列，不执行破坏性迁移。
- [x] T75 完成 32 项聚焦与相邻自动化、实际修改 CJS 语法检查、客户端构建和 Electron Main/Renderer 启动烟测；模型/API 调用 0 次，未打包。缺少用户真实样本，未声明真实模型 Step02→Step05 E2E 通过。
- [ ] ~~CP13 最终验收：两种一级目录来源、无知识库阻断、评分下级覆盖、人工填写、旧工作区兼容和普通正文无回归。~~

### Phase G：本轮客户端现场 UI 回归修复

- [x] T76 修复正文树父级节点错误显示“待响应”：父级按叶子小节聚合状态展示，叶子节点继续使用自身响应状态。
- [x] T77 修复模板设置字体控件在窄宽度下与字号重叠，以及字体下拉菜单被卡片边界裁切的问题。

### 当前阻塞与保护

- [ ] ~~原 T61/CP11 的四样本门仅属于已被替代的旧 profile/固定模板架构，不再阻塞 CHG-001；新现场验收至少需要一个明确格式样本和一个无明确格式样本，缺少时不得声称真实模型 E2E 通过。~~
- [x] 用户原有 `client/doc/1.2.0.md` 修改已识别为无关改动，本轮未触碰。
- [x] 本轮未执行 `git pull`、`git push`、`git merge`、`git rebase`、`git reset`、提交、发布或部署。

## 阶段 8：客户端发布与自动更新工作流优化

> 权威需求与验收依据：`D:\download\OpenJatoBID_发布与自动更新工作流优化方案.md`。本阶段只实现 Windows 客户端发布、私有 R2 更新、Worker 下载鉴权和管理端独立构建，不扩展 macOS、代码签名、差分更新或商业分发。

- [x] T78 统一客户端 EXE/MSI/ZIP 命名，生成精确白名单 `manifest.json` 和 SHA-256；清单脚本 2 项测试通过。
- [x] T79 实现客户端更新包下载、缓存复用和安装前 SHA-256 强制校验；4 项聚焦测试、CJS 语法检查和客户端构建通过。
- [x] T80 固定 AWS CLI + `jatoaibid/release/<version>/` 发布，增加大小/SHA-256 回读验证、latest 版本提升、失败回滚和最近两版清理；3 项 R2 脚本测试通过。
- [x] T81 将 Worker 收敛为严格许可证验签、Header-only 下载和私有 R2 规范 key；兼容管理端 DER ECDSA 签名编码，5 项路由测试通过。
- [x] T82 将客户端 GitHub Release 改为单一并发组、Draft → R2/Worker 验证 → 正式发布精确白名单流程；发布脚本/工作流 7 项测试与 YAML 解析通过。
- [x] T83 新增管理端独立手动构建、独立版本、30 天 Artifact 和临时凭据清理流程；管理端 38 项测试、构建、工作流 2 项测试与 YAML 解析通过。
- [x] T84 修复首轮只读 Review 的发布门禁问题：固定 AWS CLI v2、手动补发构建证明使用标签 SHA、正式化失败回滚 latest、Worker 校验完整许可证与离线期限、管理端 ref 在注入 Secret 前验证为 main 历史、清理保留当前与发布前稳定版，并将客户端发布收敛为稳定 `vX.Y.Z`。
- [x] CP14 本地验收：客户端 5 项更新测试与构建、管理端 38 项测试与构建、Worker 6 项测试、根目录 26 项聚焦测试、CJS/ESM/YAML/JSON/差异检查及第二轮独立只读 Review 全部通过；真实 R2/Worker/Actions 上线迁移保留为需凭据的人工步骤。

## 阶段 9：v1.4.0 R1 版本更新

> 权威需求与验收依据：`D:\download\OpenJatoBID_Codex_Version_Update_SPEC_v1.4.0_UI修订版.md`。目标仅为 `v1.4.0`；未经明确授权不推送、打 Tag、部署 Worker、发布 Release 或提升 `latest.json`。

- [x] T85 R1-0：锁定产品、更新、LAN 与结构化响应保护回归基线。
- [x] T86 R1-1/R1-2：建立 ADR 体系、上游同步 Manifest 与只读审计自动化。
- [x] T87 R1-3：统一运行时版本、Bridge 兼容与更新错误分层。
- [ ] ~~T88 R1-4 至 R1-7：已完成结构化图表 DSL、本地渲染和本地 Mermaid 导出；图片全局配额策略仍待补齐。~~
- [ ] ~~T89 R1-8 至 R1-11：已完成诊断中心与 Jato 手册 Skill；真实外部模型 Release Gate 仍待补齐。~~
- [ ] ~~T90 R1-12/R1-13：本地回归及版本提升已完成；真实外部门禁、Tag、发布与 latest 提升均未执行。~~
- [x] T91：按确认范围将系统诊断移至开发者测试页二级入口，移除通用设置中的更新来源展示和本地转图内存提示，并调整关于页隐私声明为编号左侧、内容右侧的纵向列表；GPU 行为保持不变。
- [x] T92：正文生成配置将结构化图表类型改为中文复选框并默认全选，选择结果限制编排；恢复 Mermaid 生图配置卡片且默认关闭。
- [x] T93：将正文生成配置中的结构化图表开关和上限展示名恢复为“生成 HTML 图片”“HTML 生图上限”；只读对比 `origin/upstream-main` HTML 生图链路并确认当前缺图发生在结构化 DSL 生成与统计迁移阶段，未切换或合并生图架构。
- [x] T94：按用户确认的方案 B 恢复上游 HTML 生图主链和调教规则：全文编排重新使用 `html/ai/mermaid` 与 `HTML > AI > Mermaid` 优先级，普通正文/超长正文分别走文本模型和 Agent 生成完整 HTML，源码确定性落盘与恢复后由隐藏 Electron 窗口按 1240px 本地截图、失败重试并写回权威正文；旧 v4 `chart` 计划自动重新编排，HTML 类型中文标签与上游映射一致。客户端全量 81 项测试、CJS 语法检查和 `npm run build` 通过；真实 Electron 无显式 body 的 1240×8421 长图截图通过，`yibiao-asset` PNG 导出 DOCX 验证 `word/media=1`、警告 0。未打包、未调用真实模型、未执行任何 Git 状态变更操作。

## 阶段 10：v1.4.1 OpenCode 隔离与知识库超长文件适配

> 权威需求与验收依据：`D:\download\v1.4.1-spec.md`。以本地 `main` 为产品基线、`upstream-main` 为功能参考，只做函数和能力级语义移植；禁止整分支合并、整文件覆盖、远程同步、发布和范围外优化。

- [x] T95 锁定干净基线并创建 `codex/opencode-isolation-kb-long-doc`：`main=55c5c15e439d007635cce08f15a35cabc3863e61`，`upstream-main=47aba16707cff053e4ee0734bd8fa26c90d919b5`；基线 `npm ci` 与客户端构建通过。
- [x] T96 完成 OpenCode 独立环境、内置 Skill 白名单、受控 PATH、CLI 启动前预检、Server 启动后逐字段复核、打包态内置路径锁定与诊断脱敏；隔离失败仅禁用 Agent，现有重试、Analytics、状态和工作区保护回归通过。
- [x] T97 完成知识库按模型上下文自动分段、四阶段长文档编排、多段合并与 Block 唯一归属、异常超大 Block 唯一切片、稳定 Prompt 前缀、5000ms 预热、真实 AI 队列限流和 `Promise.allSettled()` 失败语义。
- [x] T98 保留 `batchSize` IPC 兼容但由服务端忽略，删除知识库手工批次 UI，并完成设置页隔离状态的最小展示。
- [x] T99 完成 29 个测试文件 110/110、改动 CJS 语法检查、OpenCode 工具校验、客户端构建、真实 bundled sidecar、Electron Main/Renderer 启动烟测、两轮独立只读终审和 v1.4.1 测试报告；未提交、未推送、未合并、未打 Tag、未发布。

## 阶段 11：v1.4.2 知识库、设备授权与管理端升级修复

> 权威需求与验收依据：`docs/v1.4.2-spec.md`。继续使用 `codex/opencode-isolation-kb-long-doc`，只处理规格确认的六类问题；不重新实施 OpenCode 隔离、知识库长文分段、本地渲染或发布链路，未经授权不提交、不推送、不合并、不打 Tag、不发布。

- [x] T100 锁定分支、HEAD、工作树与 v1.4.1 通过基线；六类问题红灯基线分别为知识库 0/4、客户端授权 8/20、管理端 35/50，现有客户端 110/110 与管理端 38/38 保护测试在加测前通过。
- [x] T101 以应用内受控 Dialog 替换知识库文件夹原生重命名输入，并重构 Viewer Header、长文件名换行和三档响应式布局；聚焦测试 4/4 与客户端构建通过，三档视觉证据并入 T106。
- [x] T102 实现稳定 Device Code、旧 Fingerprint 兼容、Bootstrap 连接信息恢复和客户端重装后原设备登录/重新签发；客户端授权聚焦测试 32/32 通过。
- [x] T103 分离申请记录、待审批、员工、有效设备统计，实现员工内唯一设备绑定、显式旧记录合并和三台不同设备上限。
- [x] T104 实现撤销墓碑、设备额度即时释放、Bearer Watch Token 实时撤销、客户端失效落盘、重连验证和 30 天清理。
- [x] T105 固定管理端数据目录，保护三类管理员凭据状态，并实现真实文件数据库迁移前备份、失败回退和旧目录安全迁移；Node 与 Electron ABI 真实文件回归均通过。
- [ ] T106 客户端 139/139、管理端 68/68、两端构建、三档视觉验收、CJS、`git diff --check`、独立只读终审和测试报告均已完成；仍待使用当前代码生成的管理端安装包执行实体覆盖安装与重装登录，未获授权前不标记完整验收通过。

## 阶段 12：Analytics Worker Cloudflare 部署边界

> 权威需求：将 `analytics/worker` 固定为 `bidupdat` 的自包含 Worker 项目；普通部署不得创建或修改 R2、D1、KV，也不得执行远程 migration。

- [x] T107 完成 Worker、部署 helper 与三类基础设施脚本审计；明确全部 Binding、资源副作用及 `deploy-if-changed.mjs` 调用者。
- [x] T108 固定 Worker package、lockfile 和 Wrangler 4.111.0，移除普通 Worker deploy 对 setup、回填和部署 helper 的调用。
- [x] T109 固定现有 KV/D1/R2/Analytics Engine Binding、`bidupdat` 配置、Cron 与 `keep_vars`，新增配置防护和 health Binding 布尔状态测试。
- [x] T110 完成本地 `npm ci`、11 项测试、配置验证、部署 dry-run、脚本语法检查与差异检查；未执行真实部署、迁移、资源写入或 Git 状态变更。详见 `docs/secondary-development/test-reports/2026-07-18-worker-deployment-boundary-test-report.md`。

## 阶段 13：v1.4.5 投标文件质量增强

> 权威需求与验收依据：`docs/v1.4.5-spec.md`。实施基线为 `main@ac456efcc1e6f2be086a7b6528c26104ddbce2c4`，工作分支为 `codex/v1.4.5-bidq`。保留已合并的目录和正文安全 Guard；未经明确授权不推送、不合并、不发布、不打 Tag。

- [x] T111 Q1-0：已核实 `main` 与 `origin/main` 同步，`bootstrap.cjs`、guarded task service、目录 Guard、正文 Guard 和两组 Guard 回归测试存在；基线 Guard 测试 13/13 通过。
- [x] T112 Q1-1：实现公共质量数据模型、技术方案 Store 兼容与旧工作区默认值；SQLite 升级至 v20，Electron 运行时持久化回归通过。
- [x] T113 Q1-2：实现招标要求原子化、评分响应策略、否决和隐性要求提取；矩阵构建任务可在 Step 02 启动并作为进入目录生成的前置条件。
- [x] T114 Q1-3：实现增值锚点目录准入 Gate、路由校验及评分/否决/隐性要求三重反查；Step 02 新增可折叠高风险与隐性要求区。
- [x] T115 Q1-4：实现条件五级目录、深度写作元数据和目录质量 Gate。
- [x] T116 Q1-5：实现单二级子树 AI 深化、Patch 校验、候选确认、局部失效和目录 UI。
- [x] T117 Q1-6：实现 Content Plan v5、章节写作合同、输入指纹和深度/创意提示词。
- [x] T118 Q1-7：实现评分价值驱动扩写、章节统稿和多维质量审核；核心回归 35/35 与客户端构建通过。
- [x] T119 Q1-8：实现 Illustration Plan v4、创意图片类型和 Creative Brief；聚焦配图回归 13/13 与客户端构建通过。
- [x] T120 Q1-9：实现多图段落锚点、30/20/5 配置迁移及视觉 QA 状态；聚焦配图回归 18/18 与客户端构建通过。
- [ ] T121 Q1-10：已完成确定性 Fixture、42 个 Electron CJS 测试文件 163/163、构建、原生模块烟测、版本提升和实施报告；真实外部模型 Gate 与人工 Electron UI E2E 需在配置凭据和业务样本后执行，未通过前不得标记 Quality Release Gate。
- [x] T122 修复人工 E2E 中的 Step 02 任务冲突与评分响应矩阵无反馈：启动多标段识别、招标解析和评分矩阵前读取 Main 实际活跃任务并保留互斥；消除 IPC 原始错误前缀；评分矩阵将超长原文按 4 万字符级分段、显示分段/合并阶段并每 15 秒刷新等待状态。5 项聚焦 CJS 测试、CJS 语法检查、客户端构建和 `git diff --check` 已通过；需完整重启 Electron 后进行真实模型人工 E2E。
- [x] T123 修复 Step 02 误报“多标段识别中”导致招标解析按钮禁用：后台任务回放后重新加载 Main 权威工作区快照，导入文件或保存解析配置时以完整快照替换 UI 状态，避免已清除任务留在 Renderer。聚焦回归测试、客户端构建和实际 Electron 界面状态恢复已通过；未发起真实 AI 请求。
- [x] T124 修复多标段识别启动前被评分响应矩阵清空值阻断：任务初始化以 `undefined` 清空评分响应矩阵时，Store 原逻辑错误地将其送入对象校验，导致 IPC 抛出“RequirementResponseMatrix 必须是对象”、任务未持久化却残留在 Main 内存中。现改为将 `null`/`undefined` 均作为清空值持久化为 `NULL`；回归测试复现并验证该路径，未发起真实 AI 请求。
- [x] T125 修复单标段文件误走 AI 多标段识别后的状态残留：AI 最终仅识别出 1 个有效标段时，任务成功结束并清空候选标段、恢复为单标段，不自动启动招标解析；聚焦回归测试通过。
- [x] T126 按确认范围移除评分响应矩阵的整份招标文件分段并发、合并、手动入口与目录硬门槛；技术评分要求完成后自动识别重点编写项，仅以该解析结果为模型输入。识别成功时将高分项映射为重点深度编写小节；识别失败时招标文件解析正常完成，目录按普通逻辑生成。
- [x] T127 将“采购与报价”由 Markdown 表格整理改为全量结构化 JSON：固定采购项和报价规则分类 schema，Main 侧拒绝旧 Markdown、缺字段和状态不一致结果，保留全部可识别采购项与明确报价规则；4 项聚焦回归、CJS 语法、客户端构建和 `git diff --check` 已通过，未发起真实模型请求。
- [x] T128 将“采购与报价”JSON 结果改为采购项卡片与分类报价规则展示，避免通用 JSON 表格直接显示对象和数组源码；客户端构建和 `git diff --check` 已通过，待完整重启 Electron 后使用当前样本人工确认展示。
- [x] T129 修复目录下级补充因深层节点遗漏 `description` 而反复校验失败：补齐最终目录提示词的节点字段契约，缺失说明按标题回退并保留来源与评分映射校验；7 项格式目录回归、CJS 语法检查、客户端构建和 `git diff --check` 通过，待完整重启 Electron 后使用当前样本验证。
- [x] T130 修复重点编写项目录误将技术评分大类 ID 写入增值锚点：初始目录生成明确固定空锚点并确定性清除模型返回锚点，保留评分映射与重点深写约束；7 项格式目录回归、CJS 语法检查、客户端构建和 `git diff --check` 通过，待完整重启 Electron 后使用当前样本验证。
- [x] T131 分离固定人员信息表与人员能力重点编写：团队负责人、团队成员等人工叶子不再承接重点评分或深化；同章新增“团队能力分析与履约保障”承接对应评分并按既有知识库正文链路深化。8 项格式目录回归、CJS 语法检查、客户端构建和 `git diff --check` 通过，待完整重启 Electron 后使用当前样本验证。
- [x] T132 修复固定人工表格评分项被错误迁移至“如有/补充材料”目录：将评分主承载、证据材料、AI 编写与补充材料关系分离；直接对应的人工固定表格可作为一级评分主承载，评分矩阵可回写该节点；“如有/其他技术文件/补充证明材料”不得承接必选评分项，Agent 修复同时接收语义审核意见。待完整重启 Electron 后使用同类项目业绩样本验证。
- [x] T133 修复目录下级补充中的人工节点/重点深化冲突：格式目录骨架仅保留真正的人工固定项，叙述型方案、分析、措施和承诺章节自动撤销误标；人工节点不得新增、不得承担重点评分或深化；五级重点深化深度在下级补充 JSON 校验阶段前置，交由既有修复链路处理。13 项格式目录回归、CJS 语法检查、客户端构建和 `git diff --check` 通过；待完整重启 Electron 后使用现场样本进行真实模型 E2E 验证。
- [x] T134 修复人员固定表与人员能力 AI 承接的规则冲突：人员固定表不得承接技术评分主/证据/AI 映射，代码在模型返回后确定性移除误写关系，并将对应技术评分要求归入“团队能力分析与履约保障”；避免模型修复请求反复保留冲突字段。13 项格式目录回归、CJS 语法检查、客户端构建和 `git diff --check` 通过；待完整重启 Electron 后使用现场样本进行真实模型 E2E 验证。
- [x] T135 修复同类业绩等人工固定主承载与重点评分映射的规则冲突：人工固定表可作为主/证据承载，但不允许带 `mapped_scoring_point_ids` 或 AI 深化；模型误填时由代码确定性清除，评分响应矩阵仍通过主承载回写该节点。13 项格式目录回归、CJS 语法检查、客户端构建和 `git diff --check` 通过；待完整重启 Electron 后使用现场样本进行真实模型 E2E 验证。

## 阶段 14：v1.5.0 目录、重点章节与人工编制规则

> 权威需求与验收依据：`docs/v1.5.0-spec.md`。本阶段只处理目录来源、编制说明、重点章节、人工编制确认、正文责任和 Word 导出；不修改两段式提示词机制、解析项、配图、知识库、更新、管理端或发布能力。

- [x] T136 重建目录后重点章节标记：删除评分主承载、增值锚点、深度写作和五级目录门槛，按服务方案、最高可解析分值、下一档不同可解析分值标注实际目录节点。
- [x] T137 重建人工编制确认：目录初始全部 AI 编制，一级至三级复选框支持人工子树勾选与取消；目录来源和正文阶段不得自动重分配责任。
- [x] T138 收敛正文、配图、状态和导出：移除旧响应模式分流；人工叶子跳过 AI，空内容以编制说明导出，AI 节点保留两段式正文生成。
- [ ] T139 完成服务、Store、Renderer 回归和客户端构建；真实 Electron 流程需在完整重启后人工验收。

## 阶段 15：PR #10 知识库批量删除、目录弹窗优化与 Agent 重试增强

> 来源：PR #10（`Anna755zyp/main` → `migrant1124/main`），基线 `main@v1.5.0`。本阶段仅合入批量删除、CSS 优化和 Agent 重试常量；回退了格式目录生成重构、AI 错误提示重写和 `forceAgentRepair` 未完成功能。

### 知识库批量删除

- [x] T140 实现 `knowledgeBaseService.deleteDocuments()` 批量删除方法，支持一次删除多个文档。
- [x] T141 注册 `knowledge-base:delete-documents` IPC 通道并挂载到 Main 处理器。
- [x] T142 在 preload 暴露 `deleteDocuments` 并同步 `ipc.ts` 类型声明。
- [x] T143 新增 `skipped` 文档状态（无可沉淀正文的中性已跳过）。
- [x] T144 Renderer：文档列表新增 checkbox 多选、全选/取消全选、批量删除按钮与确认对话框；删除操作期间禁用所有文档操作按钮。
- [x] T145 实现批量删除/多选/全选/选中高亮/跳过状态的 CSS 样式。
- [x] T146 完成批量删除 E2E 测试、分段测试补充和 UI 回归测试。

### 目录弹窗 UI 优化

- [x] T147 扩大目录生成弹窗工作面（860→1040px / 780→820px），压缩 Agent 调试卡片，加宽知识库选择区，统一操作按钮最小尺寸。
- [x] T148 完成目录弹窗布局样式回归测试。

### Agent 重试增强

- [x] T149 新增 `FINAL_AGENT_MAX_RETRIES = 3` 常量，Agent 恢复重试次数由写死 1 改为常量控制。

## 阶段 16：上游正文生成性能与进度优化

- [x] T150 参考 `275c0c7`、`b7c7eec` 与 `e25e45a`：正文小节与编排计划改为定向 SQLite 持久化，去表格按现有文本并发上限执行，任务事件增加单调阶段进度详情；保留现有最低字数补目录/扩写与人工编制保护。CJS 语法检查、`git diff --check` 和客户端构建通过；真实模型与 Electron UI 流程待完整重启后人工验收。
- [x] T151 按确认方案接入上游完整字数控制：目录配置快照、最少/最多字数、单节范围与严格模式、人工节点只计入不改写，以及调整后的质量复核。SQLite 升级至 v21；CJS 语法检查、`git diff --check` 和客户端构建通过；真实模型结果及 Electron 交互仍需完整重启后人工验收。
- [x] T152 按界面确认稿重排目录生成配置：移除一级目录来源说明；字数输入统一为万字（2 表示 2 万字）；重排为字数控制卡片和知识库上下布局，显示预计页数，并调整字号、输入宽度与单节开关比例。客户端构建通过；待 Electron 窗口人工验收。
- [x] T153 按确认的 HTML 生图质检方案：在本地截图前检查文字旋转/倒置/镜像/缩放、重叠、前景遮挡、容器裁切、画布溢出和固定/粘性文字布局；将诊断反馈给既有两轮修复，仍失败时保留 HTML 源码且不保存 PNG。竖排文字不触发修复；不修改 AI 图、Mermaid、正文和导出流程。自动化与客户端构建验证见本轮交付记录。
- [x] T154 按确认高保真稿优化远程通知弹窗：固定头部与底部操作区，扩大窗口，正文独立滚动并显示清晰滚动条，全屏入口移出正文覆盖区域。客户端构建通过；浏览器注入长公告验证窗口 881×720px、正文可滚动、按钮不遮挡正文且底部操作区固定。待用户在完整 Electron 窗口中验收。
- [x] T155 按确认范围彻底删除 Mermaid 配图能力：移除生成、预览、导出、本地渲染、缓存、配置、诊断与依赖；既有 Mermaid 代码块按普通代码展示和导出，不保留历史兼容链路。8 项聚焦 CJS 测试与 Vite 构建通过；`npm run build` 仍被既有 `BidAnalysisPage.tsx:402` 的 ES2022 `Array.prototype.at` 类型配置问题阻断。
- [x] T156 收敛正文生成配置：移除正文配置中的最低字数及目录字数摘要，字数控制仅以目录配置快照为准；保留补目录、扩写、人工编制保护和字数复核，并移除图片数量说明卡片。Vite 构建、CJS 语法检查和 Electron 原生依赖冒烟通过；完整 TypeScript 构建仍被既有 `BidAnalysisPage.tsx:402` 的 ES2022 `Array.prototype.at` 类型配置问题阻断。

## 阶段 17：图片编排确认与生成上下文 P0

- [x] T157 持久化图片计划确认状态、AI 推荐/用户选择的全文视觉风格及用户调整后的项目；不实现最小图片比例。
- [x] T158 在全文图片编排完成后停在确认阶段，确认后仅生成保存且勾选的图片；取消全部时不插图并继续完成正文。
- [x] T159 按确认高保真基线实现图片编排确认弹窗、只读汇总、七种视觉风格推荐/选择及计划编辑。
- [x] T160 将确认后计划的视觉作用、目的、比例、评分项、价值锚点、锚点上下文、具体类型和最终视觉风格传入 HTML/AI 提示词。
- [-] T161 完成 P0 聚焦测试、构建及 Electron 用户测试门；P0 测试通过前不启动 P1。CJS 语法检查、Vite 构建和差异检查通过；完整 TypeScript 构建仍受既有 `BidAnalysisPage.tsx:402` 阻断，既有保护测试也与此前人工编制规则变更不一致。待 Electron 人工验收。

## 阶段 18：图片生成质量优化 P1

- [x] T162 生成并持久化视觉节奏诊断，在图片编排确认页显示只读建议；不得自动改变图片计划。4 项图片编排聚焦测试、CJS 语法检查、Vite 构建和差异检查通过；待 Electron 用户测试确认建议可读性。
- [ ] T163 为流程图、时间轴、矩阵图、鱼骨图、组织架构图、循环图和金字塔图补充 HTML 类型化模板约束，保留既有 HTML 质量检查。
- [ ] T164 实现稳定图号、仅含图号和标题的图注、必要的图后说明，并移除图片前置引导句。
- [ ] T165 在既有模型具备图片输入能力时执行非阻断 AI 图片视觉复核；无视觉模型时保留人工复核状态且不阻断生成。
- [ ] T166 完成 P1 聚焦测试、构建和 Electron 用户测试门；不触碰既有 `BidAnalysisPage.tsx:402` 基线类型错误。
- [-] T167 修复 HTML 图片本地渲染缺少 `pathToFileURL` 导入导致所有 HTML 图片失败的问题；人工编辑章节不计入图片覆盖或视觉节奏诊断。21 项本地渲染、HTML 生图和编排聚焦测试、CJS 语法检查、Vite 构建及差异检查通过；待完整重启 Electron 后实际生成 HTML 图片并导出 Word 验收。
- [-] T168 删除图片数量设置及硬上限，改为模型自主规划且 AI/HTML 独立保留；修正人工章节完成统计和成功后的“重新生成正文”按钮。图片编排聚焦测试通过；待完整重启 Electron 后人工核对已确认的配置弹窗与完成态统计。
- [x] T169 将 `BidAnalysisPage.tsx` 中 ES2022 的 `Array.prototype.at(-1)` 改为 ES2020 兼容的数组下标读取；完整客户端构建和差异检查通过。
- [-] T170 吸收 `f04eabc` 的安全版局部精确扩写；保持人工编制保护、受保护 Markdown 范围和原方案覆盖修复契约。5 项内容计划与扩写聚焦测试、CJS 语法检查和完整客户端构建通过；待 Electron 真实扩写验收。
- [-] T171 吸收 `7cb3512` 的 HTML 探测与最终截图分离；不得放松现有两轮修复失败后的 PNG 拒绝规则。19 项内容计划、HTML 生图和本地渲染聚焦测试、CJS 语法检查和完整客户端构建通过；待 Electron 真实生图及 Word 导出验收。
- [x] T172 修复内容计划写入/读取时丢失 `writing_profile`、使 `deep` 回退为 `standard` 的缺陷；内容计划聚焦测试和完整客户端构建通过。

## 阶段 19：CHG-002 目录叶子归类与可选五级正文节点

- [-] T173 保留来源下级层级，约束首次目录生成中模型新增内容采用三级主题与四级叶子；五级仅用于现有重点章节；同一二级目录新增无子节点三级目录最多 5 个。10 项目录与质量聚焦测试、CJS 语法检查、完整客户端构建和差异检查通过；待 Electron 真实目录生成验收。
- [-] T174 使目录字数规划、知识库补目录和正文最低字数补目录遵守同一归类和重点章五级规则，不移动来源、人工或已有正文节点。19 项目录与正文聚焦测试、CJS 语法检查、完整客户端构建和差异检查通过；待 Electron 三路径验收。
- [ ] T175 限制模型与手工新增目录不超过五级，并回归正文生成、字数统计、图片编排和 Word 导出的四级/五级叶子行为。
- [ ] T176 完成聚焦测试、客户端构建和 Electron 用户测试门；不迁移既有目录，不打包、提交或推送。
